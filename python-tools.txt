# Standalone CLI tools installed via `uv tool install`
# Each gets its own isolated environment
# graphify is installed explicitly in setup.sh (version-pinned from claude/skills/graphify/.graphify_version)
